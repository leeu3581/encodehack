// packages/A/jest.config.js (and packages/B/jest.config.js)

const baseConfig = require('../../jest.config')

module.exports = {
  ...baseConfig,
  displayName: '@binance/w3w-qrcode-modal', // Replace with the package name
  roots: ['<rootDir>/src'],
}
