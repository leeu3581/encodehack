/* eslint-disable @typescript-eslint/no-var-requires */
const baseConfig = require('../../jest.config.js')

module.exports = {
  ...baseConfig,
  displayName: '@binance/w3w-ethereum-provider', // Replace with the package name
  roots: ['<rootDir>/src'],
}
